# 저장 상태 표시 수정 보고서

## 변경 내용

- `src/content/editor.tsx`에 `not-saved`, `needs-save`, `saved` 세 가지 저장 상태를 정의했습니다.
- `dirty`를 우선해 변경사항이 있으면 항상 `needs-save`로 표시하고, 변경사항이 없을 때 `draftExists`로 저장 완료 여부를 판정합니다.
- 버튼 라벨과 상태줄이 동일한 `saveState`를 사용하도록 정리했습니다.
- 저장된 초안이 있는 경우에만 `savedAt`을 `ko-KR` 형식의 마지막 저장 시각으로 표시합니다. JSON을 불러온 직후에는 브라우저 저장 전이므로 파일의 시각을 저장 완료로 표시하지 않습니다.
- 저장 버튼에는 `disabled` 처리를 추가하지 않아 세 상태 모두에서 클릭해 저장할 수 있습니다.

## 상태별 표시

| 상태 | 판정 | 저장 버튼 | 상태줄 |
| --- | --- | --- | --- |
| 아직 저장한 적 없음 | `dirty=false`, `draftExists=false` | `저장` | `아직 저장하지 않았습니다.` |
| 변경사항 저장 필요 | `dirty=true` | `저장 필요` | `저장하지 않은 변경사항이 있습니다.` (저장된 초안이 있으면 뒤에 `마지막 저장 {savedAt}` 추가) |
| 저장 완료 | `dirty=false`, `draftExists=true` | `저장됨` | `저장된 초안을 사용 중입니다. 마지막 저장 {savedAt}` |

작업 결과 메시지가 있을 때도 상태줄에 현재 상태 문구를 함께 표시해, 작업 피드백이 상태 판단을 가리지 않도록 했습니다. 기본값 복원 시에는 이전 작업 메시지를 지워 초기 상태 문구가 바로 표시됩니다.

## 검증 결과

- `npm run build` 통과 (`tsc -b && vite build`)
- `npm run lint` 통과
- `git diff --check` 통과
